[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --cov=agents
    --cov=tools
    --cov-report=html
    --cov-report=term-missing
    # Coverage requirement will be enforced after tests are written
